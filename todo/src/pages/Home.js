import React from 'react';
import { Link } from 'react-router-dom';

const Home = () => {
  return (
    <div className="min-h-screen bg-gradient-to-r from-green-300 to-blue-400 p-8 flex flex-col items-center justify-center space-y-6">
      <h1 className="text-5xl font-bold text-white mb-8">Welcome to the Activity Tracker App</h1>
    
      </div>
  
  );
};

export default Home;
